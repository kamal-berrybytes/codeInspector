apiVersion: v1
kind: Secret
metadata:
  name: jwt-signing-key
  namespace: agentgateway-system
type: Opaque
stringData:
  # The shared secret used for HS256 signing/verification
  # Must match JWT_SIGNING_SECRET in apiServer
  signing-key: "default-signing-secret-change-me"